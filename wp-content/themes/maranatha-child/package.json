{
	"name": "maranatha-child-dev",
	"version": "0.0.0",
	"private": true,
	"description": "Dev-only JS toolchain for the Maranatha Child theme (First Church Seattle). NOT shipped — production runs no build step; see ops/deploy.sh excludes.",
	"type": "module",
	"scripts": {
		"test": "vitest run --passWithNoTests",
		"test:watch": "vitest",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"e2e": "playwright test",
		"e2e:install": "playwright install chromium"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@playwright/test": "^1.49.1",
		"happy-dom": "^15.11.7",
		"vitest": "^2.1.9"
	}
}
